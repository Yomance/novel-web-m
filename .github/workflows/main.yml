name: Test

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]

    steps:
    - uses: actions/checkout@v3

    - name: Use Node
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Build 🚀
      run: |
        npm install
        npm run build

    - name: publish
      run: |
        cd m
        git init
        git add .
        git commit -m ''
        git branch -M master
        git remote add origin https://github.com/Yomance/novel-m-vue-build.git
        git push -u origin master
